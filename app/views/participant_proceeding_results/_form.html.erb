<%= form_for(@participant_proceeding_result) do |f| %>
  <% if @participant_proceeding_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participant_proceeding_result.errors.count, "error") %> prohibited this participant_proceeding_result from being saved:</h2>

      <ul>
      <% @participant_proceeding_result.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :participant_proceeding %><br>
    <%= f.number_field :participant_proceeding %>
  </div>
  <div class="field">
    <%= f.label :results %><br>
    <%= f.text_area :results %>
  </div>
  <div class="field">
    <%= f.label :checked %><br>
    <%= f.check_box :checked %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
