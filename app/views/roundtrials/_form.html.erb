<%= form_for(@roundtrial) do |f| %>
  <% if @roundtrial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roundtrial.errors.count, "error") %> prohibited this roundtrial from being saved:</h2>

      <ul>
      <% @roundtrial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.text_field :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
